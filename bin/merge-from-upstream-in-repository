#!/bin/sh

# Use bin/merge-from-upstream to do this to the sandbox and to
# all of your 'npm link'ed modules. You don't need this directly.

echo "Merging from upstream in $1"
cd $1 &&
git fetch upstream &&
git merge upstream/master &&
echo "Merged without errors for $1"


